/**
 * Main package of InstaList.
 *
 * Since the app is written in a MVC-pattern, it contains only the packages {@code model},
 * {@code view} and {@code controller}. Additionally, for tests exist more packages which should not
 * be shipped.
 */
package org.noorganization.instalist;