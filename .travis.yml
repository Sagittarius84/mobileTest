language: android
android:
    jdk:
      - openjdk8
    components:
      - tools
      # the current build tools version used by this project
      - build-tools-23.0.3
      # the sdk used to compile the project
      - android-23
      - extra-android-support-23.4.0
      - extra-android-m2repository-23.4.0
      
       
    before_install:
      - chmod +x gradlew